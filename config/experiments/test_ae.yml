encoder:
  in_channels: 3
  latent_dim: 64
  image_size: 512
  global_norm: null
  global_act: null
  global_dropout: 0.0
  layers:
    - out_channels: 32
      kernel_size: 3
      stride: 2
      padding: 1
      norm: batch
      act: relu
    - out_channels: 64
      kernel_size: 3
      stride: 2
      padding: 1
      norm: batch
      act: relu
    - out_channels: 64
      kernel_size: 3
      stride: 2
      padding: 1
      norm: batch
      act: relu

decoder:
  out_channels: 3
  latent_dim: 64
  image_size: 512
  # Decoder layers are auto-generated from encoder