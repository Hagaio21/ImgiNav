# Inference configuration file for diffusion model sampling
# This file contains paths to all required configs and checkpoints

diffusion:
  config: experiments/diffusion/clip/regular_rooms/small_down_bottleneck_text_only.yaml
  checkpoint: checkpoints/diff_clip_regular_rooms_small_down_bottleneck_text_only_checkpoint_best.pt

autoencoder:
  # For standalone checkpoints, autoencoder config is embedded in diffusion checkpoint
  # These paths are only used if the diffusion checkpoint doesn't contain decoder config
  config: null
  checkpoint: checkpoints/vae_checkpoint_best.pt

